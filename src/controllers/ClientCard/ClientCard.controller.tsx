import { ClientCardComponent  } from "../../components/ClientCard/ClientCard.component";
import React from "react";

export const ClientCard = (props:any) => {
    return(
        <ClientCardComponent {...props} />
    )


}