import React from 'react'
import { AppointmentCardComponent } from './../../components/Appointment'

export const AppointmentCard = (props: any) => {
  return (
    <AppointmentCardComponent {...props} />
  )
}
